# Copy this file to a machine-local tube_slots.yaml and replace path with the
# absolute dataset root. Do not commit private or very large datasets here.
path: C:/datasets/tube_slots
train: images/train
val: images/val
test: images/test

# This order is a runtime contract. Do not rename or swap the classes.
names:
  0: empty_hole
  1: tube_cap
